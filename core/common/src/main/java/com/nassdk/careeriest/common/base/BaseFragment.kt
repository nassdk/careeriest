package com.nassdk.careeriest.common.base

import androidx.fragment.app.Fragment

/**
 * Need just for right hierarchy and abstraction, must not be extended.
 * Use Screen realizations. See - [BaseScreen] & [BaseVmScreen]
 */
abstract class BaseFragment : Fragment()